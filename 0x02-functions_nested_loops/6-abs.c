#include "main.h"

/**
 * _abs -  computes the absolute value of an integer
 * @n: input number
 * Return: - returns absolute value of integer
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = -n;
	}
	return (n);
}
